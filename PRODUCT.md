# 产品设计文档：《电子书阅读器》

## 一、产品定位（一句话）

一个**纯静态、零依赖、双击即用**的本地 TXT 电子书阅读器，阅读体感接近桌面阅读 App，所有数据只存本地。

## 二、目标用户

- 习惯在本地/浏览器里读 TXT 网文的读者（起点/晋江等下载章节）
- 不想为读书装 App、在意隐私（本地文件不上传）、想要轻量工具的人

## 三、整体方向

- **核心三条做到顺滑**：导入 → 舒适阅读 → 记住进度。
- **数据本地化**：无后端、无账号，进度/偏好存 `localStorage`，文件不出本机。
- **渐进式演进**：先把 TXT 读熟，再逐步扩格式、书库、移动端。

## 四、核心功能（三档）

**MVP（本次做）**
- 导入本地 TXT 文件
- 分页阅读 + 字号调节（14–28px）
- 阅读进度记忆——下次打开自动恢复
- 章节目录导航（识别"第 X 章/回/节/卷、Chapter N"）
- 日间/夜间主题切换

**V1（后续）**
- EPUB / PDF 支持
- 书库管理：书架、最近阅读、多书
- 页内跳转：进度条/滑杆跳页
- 中文 TXT 常见编码（GBK 等）完整兼容

**远期**
- 笔记 / 高亮 / 摘录
- 移动端 PWA、桌面封装（Tauri）
- 可选云同步（与"本地化"方向权衡）

## 五、技术方向

- 纯静态 HTML/CSS/JS，`file://` 直接打开可运行（不用 ES 模块，规避浏览器拦截）
- 章节识别 + 分页计算做成**可测试的纯逻辑模块**（浏览器用 `window` 全局、测试用 `module.exports`，双导出）
- 只用浏览器原生能力：`FileReader` / `localStorage` / CSS 变量主题

## 六、MVP 完成标准

能导入一本 TXT → 分页舒适阅读、可调字号 → 目录可跳章节 → 夜间主题 → 关掉再开自动回到上次位置；配套单元测试全绿。
